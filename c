#include<stdio.h>

int main()
{
    int a,c,x; 
    //a area, p perimeter
    printf("x enter the number from the user:");
    scanf("%d", &x);
       a=x*x;
       printf("a=x*x, %d*%d=%d\n",x,x,a);


       printf("x degerini gir:");
       scanf("%d", &x);
         p=4*x;
         printf("p=4*x, 4*%d=%d", x,p);
    return 0;     
}
